#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

/// \fn void FormatearCadena(char[])
/// \brief Formatea cadenas de caracteres de una forma dada.
/// \param Es la cadena de caracteres a formatear.
void FormatearCadena(char []);

/// \fn void ConcatenarStrings(char[], char[], char[])
/// \brief Concatena cadenas de caracteres.
/// \param Es la primer variable ingresada por el usuario.
/// \param Es la segunda variable ingresada por el usuario.
/// \param Es la variable resultado de la concatenacion de las anteriores.
void ConcatenarStrings(char [], char [], char []);
